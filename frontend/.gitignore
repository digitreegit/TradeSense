tsconfig.tsbuildinfo

# Vite reads env files from this directory (see vite.config.ts).
# Root `.gitignore` also ignores `.env` repo-wide; listed here for clarity.
.env
.env.local
.env.*.local
!.env.example
