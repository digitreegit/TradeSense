# Same values as backend SUPABASE_URL / SUPABASE_ANON_KEY (Supabase Dashboard → Settings → API).
# Only names prefixed with VITE_ are exposed to the browser bundle.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
