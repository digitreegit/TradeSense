# Vite `envDir` is the repo root — only `../.env` is loaded, not this file.
# Copy repo root `.env.example` → `.env` and set `VITE_*` there (see DEVELOPMENT.md).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
